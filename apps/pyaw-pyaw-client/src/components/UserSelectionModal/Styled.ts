import styled from "styled-components";

export const UserSelectionModalWrapper = styled.div`
  display: flex;

  left: 50%;

  outline: 0;

  position: absolute;

  top: 50%;
  transform: translate(-50%, -50%);
`;
